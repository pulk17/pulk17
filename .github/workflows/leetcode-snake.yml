name: Generate LeetCode Snake

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      # Install Python
      - name: Set up Python
        uses: actions/setup-python@v3
        with:
          python-version: 3.10

      # Install dependencies
      - name: Install dependencies
        run: pip install requests

      # Fetch LC activity → data/activity.json
      - name: Fetch LeetCode Activity
        run: python scripts/fetch_leetcode_activity.py pulk1tt

      # Generate snake SVG (using custom grid)
      - name: Generate Snake SVG
        uses: Platane/snk@v3
        with:
          data_source: data/activity.json
          outputs: |
            dist/leetcode-snake.svg
            dist/leetcode-snake-dark.svg?palette=github-dark

      # Push to output branch
      - name: Push to Output Branch
        uses: crazy-max/ghaction-github-pages@v2.1.3
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
